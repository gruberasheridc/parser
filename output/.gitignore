# Ignore everything in this directory. We use this ignore file, so we will be able to commit the empty folder to
# the Git repository. Since, the folder is important for the projects' structure.
*
# Except this file
!.gitignore